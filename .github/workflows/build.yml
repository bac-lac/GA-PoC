name: Build Placeholder

on:
  workflow_dispatch:

  schedule:
    # Every Sunday at 05:00 UTC
    - cron: '0 5 * * 0'

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Build placeholder
        run: |
          echo "=================================="
          echo "Placeholder build pipeline"
          echo "Repository: ${{ github.repository }}"
          echo "Branch: ${{ github.ref_name }}"
          echo "Run Date: $(date -u)"
          echo "=================================="
