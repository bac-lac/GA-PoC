name: Create tfvars file
decription: Create a tfvars file that's going to be used by terraform plan/apply.
inputs:
  account:  # id of input
    description: 'Who to greet'
    required: true
    default: 'World'
runs:
  using: "composite"
  steps:
    - name: Setup Terraform variables
      id: vars
      run: |-
        cat > pipeline.auto.tfvars2 <<EOF
        ACCOUNT = "${{ inputs.account }}"
        ADMIN_DB_PASSWORD = "${{ inputs.account }}"
        ADMIN_DB_USERNAME = "${{ inputs.account }}"
        BRANCH_NAME = "${{ env.BRANCH_NAME }}"
        DB_PASSWORD = "${{ inputs.account }}"
        DB_USERNAME = "${{ inputs.account }}"
        ECR_IMAGE = "${{ env.IMAGE_REPO_TAG }}"
        ENV = "${{ inputs.account }}"
        FORCE_REFRESH = "${{ inputs.account }}"
        MFT_CLUSTER = "${{ inputs.account }}"
        EOF
      shell: bash
    - name: Print Terraform variables
      id: print
      run: cat pipeline.auto.tfvars2
      shell: bash